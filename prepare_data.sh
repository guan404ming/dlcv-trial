#!/bin/bash
# This script extracts the images and depths tarballs.
# Make sure images.tar.gz and depths.tar.gz are in the same directory as this script.

tar -xvf images.tar.gz
tar -xvf depths.tar.gz
